const Main = () => {
    return (
        // Main
        <section className="grow">
            <header>header</header>
            <main>main</main>
        </section>
    );
}

export default Main;